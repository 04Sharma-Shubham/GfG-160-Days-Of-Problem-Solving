    void pushZerosToEnd(int[] arr) {
        int zero=0;
        int n=arr.length;
        for(int i=0;i<arr.length;i++){
            if(arr[i]!=0){
                int temp=arr[i];
                arr[i]=arr[zero];
                arr[zero]=temp;
                zero++;
            }
        }
    }